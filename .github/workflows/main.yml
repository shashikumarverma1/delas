name: EAS Build & Submit to Play Store

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Login to Expo
        run:  eas build -p android --profile production --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # --- 🔒 FOR PLAY STORE SUBMISSION (COMMENTED) ---
      # - name: Write Google Play JSON
      #   run: echo "${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}" > google-play-service.json

      # - name: Build Android App Bundle
      #   run: eas build -p android --profile production --non-interactive
      #   env:
      #     EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # - name: Submit to Play Store
      #   run: eas submit -p android --profile production --non-interactive
      #   env:
      #     EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # --- ✅ NEW: BUILD APK ONLY ---
      # - name: Build APK (Standalone Android App)
      #   run: eas build -p android --profile production --non-interactive
      #   env:
      #     EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
